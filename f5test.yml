---
- name: Test F5 playbook
  hosts: localhost
  tasks:
    - name: Add the user johnd as an admin
      bigip_user:
          server: "{{ f5 }}"
          user: "{{ user }}"
          password: "{{ password }}"
          username_credential: "johnd"
          password_credential: "password"
          full_name: "John Doe"
          partition_access: "all:admin"
          update_password: "on_create"
          state: "present"
