---
# This playbook runs a ranorex test
- name: RunRanorexTest
  hosts: all 

  tasks:
  - name: RunRanorexCommand
    win_command: c:\pstools\psexec.exe -h -accepteula -i 1 {{ testexe }} {{ testparam }}
    #win_command: schtasks.exe /Run /TN {{ testparam }}
