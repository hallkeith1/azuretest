---
# This playbook runs a ranorex test
- name: RunRanorexTest
  hosts: all 

  tasks:
  - name: RunRanorexCommand
    win_command: 'c:\pstools\psexec.exe -accepteula -i 0' "{{ testexe }} {{ testparam }}"
